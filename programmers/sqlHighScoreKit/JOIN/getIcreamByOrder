SELECT FLAVOR FROM (
    ( SELECT FLAVOR, SUM(TOTAL_ORDER) AS A FROM FIRST_HALF GROUP BY FLAVOR)
    UNION ALL
    ( SELECT FLAVOR, SUM(TOTAL_ORDER) AS A FROM JULY GROUP BY FLAVOR )
) A
GROUP BY FLAVOR
ORDER BY SUM(A) DESC
LIMIT 3
